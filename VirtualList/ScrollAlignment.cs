namespace VirtualList;

    /// <summary>ScrollToIndex 主轴对齐。</summary>
    public enum ScrollAlignment
    {
        /// <summary>置顶（水平：置左）。</summary>
        Start,

        /// <summary>居中。</summary>
        Center,

        /// <summary>置底（水平：置右）。</summary>
        End,
    }